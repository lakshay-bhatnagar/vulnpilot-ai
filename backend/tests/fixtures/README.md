# Parser fixtures

The XML and JSON files are representative scanner exports. The PCAP fixture is generated by
`test_pcap_parser.py` with Scapy because packet captures are binary artifacts; the test exercises
the same bytes passed to the upload parser.
